# =============================================================================
# Environment variables for production deployment
# Copy this file to docker/.env and fill in the values with production secrets
# =============================================================================

# --- Stack Configuration ---
# COMPOSE_PROJECT_NAME differenzia i container tra ambienti sullo stesso server.
# Usare "seed-production" per production e "seed-staging" per staging.
COMPOSE_PROJECT_NAME=seed-production

# Porte esterne Nginx (production: 80/443, staging: 8080/8443)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Porta esterna Seq (production: 8081, staging: 8082)
SEQ_PORT=8081

# --- PostgreSQL ---
POSTGRES_DB=seeddb
POSTGRES_USER=seed
POSTGRES_PASSWORD=<strong-password-here>

# --- ASP.NET Core API ---
ASPNETCORE_ENVIRONMENT=Production
ConnectionStrings__DefaultConnection=Host=postgres;Database=seeddb;Username=seed;Password=<strong-password-here>
JwtSettings__Secret=<random-string-at-least-32-characters>
AllowedHosts=*

# --- SMTP (opzionale — se vuoto, le email vengono loggate in console) ---
Smtp__Host=
Smtp__Port=587
Smtp__Username=
Smtp__Password=
Smtp__FromEmail=noreply@yourdomain.com
Smtp__FromName=Seed App
Smtp__Security=StartTls

# --- SuperAdmin seeding (first startup — remove after first deploy) ---
SuperAdmin__Email=<admin-email-here>
SuperAdmin__Password=<strong-admin-password-here>
SuperAdmin__FirstName=Super
SuperAdmin__LastName=Admin

# --- VPS Deployment (docker-compose.deploy.yml) ---
# DOMAIN_NAME is used by nginx for server_name
DOMAIN_NAME=yourdomain.com
GHCR_OWNER=your-github-username
IMAGE_TAG=latest

# URL base dell'applicazione (usato nelle email e redirect)
# Includere sempre il protocollo: https://yourdomain.com oppure https://staging.yourdomain.com
CLIENT_BASE_URL=https://yourdomain.com
